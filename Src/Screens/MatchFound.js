import { View, Text, ImageBackground,Image } from 'react-native'
import React from 'react'
import {Images} from '../Constant/Images';
const MatchFound = () => {
  return (
    
      <ImageBackground source={Images.Matchback} style={{flex:1}}/>
    
  )
}

export default MatchFound